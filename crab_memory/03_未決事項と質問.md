# 未決事項と質問

## 決定記録（2026-09-18 回答済み）

| # | 質問 | 回答 | 設計への影響 |
|---|---|---|---|
| Q1 | 最初の対象文章 | **論説文** | 関係型は主張・根拠系を優先。小説向けの出来事・人物ノードは後回し |
| Q2 | 利用者 | **開発者2人（自分たち用）** | UI は最小限。設定（API キー等）は各自がブラウザに保持でよい |
| Q3 | 文章の長さ | **数百〜数千字** | 分割処理・非同期処理は不要。1文章＝1回の処理で済む |
| Q4 | 図の目的 | **俯瞰が主。追跡も必須** | 全体図（議論マップ）を主画面にし、本文パネルとの相互ハイライトで追跡を担保 |
| Q5 | 動作環境 | **Web。言語は問わない** | ブラウザだけで完結する静的 Web アプリにする（→ 04） |
| Q6 | 外部 AI API | **利用可。ただし費用はかけたくない** | サーバーを持たない。AI 層は差し替え可能にし、ルールのみ版を先に作る（→ 02 §5） |
| Q7 | 手動修正 | **デバッグ機能として含める** | 修正結果を JSON で保存・書き出しできるようにし、評価データにも流用する |
| Q8 | 「動き」の意図 | **動きを見せたい方に近い** | 動詞ごとのアニメではなく、関係型ごとの動き（押す・支える・反転する）で表す（→ 01 §5.1） |
| Q9 | 画像出力 | **静止画は必要。動きは対象外** | SVG で描画し、SVG / PNG に書き出す。アニメはブラウザ内のみ |
| Q10 | 対象言語 | **日本語のみ** | 文分割・接続表現辞書は日本語専用でよい |

## 未決の質問（第2ラウンド）

| # | 質問 | なぜ聞くか | 当面の仮定 |
|---|---|---|---|
| Q11 | 「費用をかけたくない」は完全ゼロか、開発中に各自数十〜数百円程度なら許容か | 完全ゼロならブラウザ内 LLM かルールのみ。少額可なら外部 API を各自のキーで使える（02 §5 の試算参照） | 少額なら許容。まずルールのみ版で費用ゼロ |
| Q12 | 論説文で「動かす」対象は「論理の操作」（根拠が主張を支える、反論が押し返す等）でよいか | 論説文には出来事の動きがほとんど無いため、動きのメタファーを関係型に割り当てる案にした（01 §5.1） | 関係型ごとの動きで表す |
| Q13 | 技術スタック案（TypeScript + Vite + React、描画は SVG）でよいか。共同開発者の得意言語は | 04 の提案。2人で触るのでどちらも書ける言語がよい | 提案どおり |
| Q14 | ホスティングは GitHub Pages でよいか | 静的サイトなら無料で済む | GitHub Pages |
| Q15 | 手動修正の保存先は「JSON ダウンロード ＋ ブラウザ保存（localStorage）」でよいか | サーバーが無いので共有はファイル経由になる | JSON ファイル |

## 次にやること

1. Q11〜Q15 の回答をもらう（並行して 2 以降は進められる）。
2. 関係型の最終セットを決め、中間表現の JSON スキーマを書く（01 §3）。
3. サンプルの論説文 3〜5 本を用意し、手で「正解の図」を描く。スキーマと図の表現方法を検証する。
4. 04 の構成でステップ0（ルールのみ版）の最小実装に入る。
